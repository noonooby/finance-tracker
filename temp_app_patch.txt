        {currentView === 'activity' && (
          <ActivityFeed
            darkMode={darkMode}
            onUpdate={loadAllData}
          />
        )}
        {currentView === 'settings' && (
          <Settings
            darkMode={darkMode}
            onUpdate={loadAllData}
          />
        )}
